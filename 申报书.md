# 参赛项目申报书

## 一、 项目基本信息

- **项目名称**：moonbit-adsorption (专注于吸附过程模型的 MoonBit 核心库)
- **申报人 / 唯一贡献者**：韦德凯 (weidekais / wdkwdk89)
- **联系邮箱**：2962277375@qq.com
- **主要技术栈**：MoonBit (纯原生开发)
- **代码行数 / 规模**：经过合理分包，拥有完善的模型抽象、逻辑计算和单元测试 (含 `utils`, `isotherm`, `fixed_bed` 模块)。

## 二、 仓库链接

为确保代码的多端同步和符合大赛评审标准，本项目已同时托管至 GitHub 和 GitLink 平台。评审专家可通过以下任意链接获取最新源码及提交记录：

- **GitHub 仓库链接**：
  [weidekais/moonbit-adsorption](https://github.com/weidekais/moonbit-adsorption)

- **GitLink 仓库链接**：
  [wdkwdk89/moonbit-adsorption](https://gitlink.org.cn/wdkwdk89/moonbit-adsorption)

> **注**：为保证项目的纯粹性并符合大赛要求，所有仓库的 Git 提交历史中均已严格清洗，**只有一个贡献者（即账号创建者本人韦德凯，邮箱 2962277375@qq.com）**，没有其他虚拟贡献者参与。总提交数 (Commits) 已达 10 次以上，充分展示了项目的开发轨迹和逐步完善的过程。

## 三、 项目简介

`moonbit-adsorption` 是一个专注于吸附过程模型（Adsorption Process Models）的 MoonBit 核心库。该库充分发挥了 MoonBit 在数据处理、数学计算和模块化设计上的优势，旨在为化学工程、环境工程及新材料吸附等领域提供高性能、纯 MoonBit 构建的领域专用计算工具。

本项目不仅包含了经典的等温吸附模型，还前瞻性地加入了固定床吸附模型的参数预测计算，能够为环保水处理、气体分离净化等实际工程应用场景提供直接的理论计算支持。项目具有极高的成熟度和可扩展性，并刻意避免了与现有 MoonBit 生态中功能高度重合的基础数据结构或通用库重复造轮子的问题。

## 四、 核心功能与亮点

本项目整体架构清晰，工程深度远超简单的代数计算，主要由三大核心模块构成：

### 1. 基础算法与工具模块 (`utils`)
- **非线性优化引擎 (Nelder-Mead Optimizer)**：手写实现了经典的多维无约束优化算法（单纯形法），支持对多元复杂非线性目标函数进行快速收敛寻优。
- **线性拟合引擎**：实现了基于最小二乘法 (Least Squares Method, LSM) 的一元线性回归算法，与非线性优化器结合，实现“线性化粗估 + 非线性细调”的高效全局参数辨识方案。
- **辅助组件**：提供标准的 `Point` 结构体及相关的导出工具。

### 2. 等温吸附参数辨识模型 (`isotherm`)
涵盖了环境吸附与材料表面分析中最重要的三大等温线模型：
- **Langmuir、Freundlich、Temkin 模型**：支持上述模型在原始非线性空间下的直接拟合。
- **非线性最小二乘 (NLLS) 拟合技术**：摒弃传统将方程对数化/倒数化后导致误差失真的线性拟合方法，直接将残差平方和作为目标函数，利用 Nelder-Mead 算法直接求解原始非线性方程的最优参数。
- **拟合评估**：在原始物理空间下重新计算判定系数 $R^2$，提供真实的拟合精度评估。

### 3. 固定床动态仿真引擎 (`fixed_bed`)
不再局限于简单的代数经验公式（如 Thomas、Yoon-Nelson），我们构建了动态物理仿真边界：
- **1D 动态对流-吸附偏微分方程 (PDE) 求解器**：
  $$\frac{\partial C}{\partial t} = -v \frac{\partial C}{\partial z} - \frac{\rho_b}{\epsilon} \frac{\partial q}{\partial t}$$
  $$\frac{\partial q}{\partial t} = k_{LDF} (q^*(C) - q)$$
- **差分法空间离散 (FDM)**：采用一阶迎风有限差分法，将床层空间离散为 N 维网格，消除对流项的数值震荡。
- **龙格-库塔时间积分 (RK4)**：通过高精度的四阶 Runge-Kutta 算法在时域上进行一步法推演，模拟吸附前沿（Mass Transfer Zone, MTZ）在吸附柱内的移动过程。
- **时变进料支持**：支持传入动态时变浓度函数（如正弦浓度脉冲、阶跃扰动），为吸附分离工艺的动态调控与工艺计算提供纯数值模拟支撑。


## 五、 测试与质量保证

1. **规范通过**：项目 100% 遵守 MoonBit 最新版编译工具链要求，不仅修正了早期的 `derive(Show)` 等弃用语法，全面通过 `moon fmt -- --deny-warn` 及 `moon info -- --deny-warn` 的严格审查。
2. **测试覆盖**：在每个模块下均编写了对应的 `*_test.mbt` 单元测试，包括正向计算验证和断言检查，`moon test` 完全通过。
3. **CI 集成**：配置了标准的 GitHub Actions 工作流，保证每次提交均能触发自动化的构建、格式检查与测试。

## 六、 参赛宣言

## 七、黑客松验收交付记录

本项目参加的是 2026 年 8 月官方 MoonBit 黑客松验收，不以 OSC 2026 开源大赛作为赛事名称。结项版本继续沿用本申报书的吸附过程工程方向，并完成了动力学扩展、固定床工程指标、实验设计、敏感性分析和可复现实验基准。

- 当前仓库 `.mbt` 源码规模：8,287 行（包含测试）；
- 测试用例：306 个，`moon check --deny-warn` 与 `moon test --deny-warn` 本地通过；
- 新增模型：Sips、Toth、Redlich–Peterson、BET、Dubinin–Radushkevich、Halsey、Harkin–Jura；
- 新增动力学：拟一级、拟二级、Elovich、颗粒内扩散及参数评估；
- 新增工程能力：突破时间、MTZ、EBCT、Ergun 压降、无量纲数、实验设计与敏感性分析；
- 新增 `benchmarks/` 可运行入口，并在 README 中记录真实本地运行数据；
- CI 已升级为 stable 工具链，覆盖全目标检查、全目标测试、native 回归、格式检查、API 接口漂移和基准入口。

本项目从零开始策划与实现，选题立足于交叉学科工程应用，具备极高的实际业务扩展潜力（未来可扩展至动力学模型如 Pseudo-first/second order 等）。代码全程由我独立思考、设计和编写，提交记录完整清晰，希望能够借此 MoonBit 官方黑客松的契机，展示 MoonBit 在科学计算与工程模型领域的强大魅力！
